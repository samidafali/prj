@model IEnumerable<ProjetEpîcerie.Models.TraduitAcceuil>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Langue.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.btnDeconnecter)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.txt1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.txt2)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Langue.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.btnDeconnecter)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.txt1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.txt2)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTraduitAcceuil }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTraduitAcceuil }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTraduitAcceuil })
        </td>
    </tr>
}

</table>
