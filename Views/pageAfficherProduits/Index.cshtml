@model IEnumerable<ProjetEpîcerie.Models.pageAfficherProduits>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Langue.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BtnDescon)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BtnVoir)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BtnAjoutPanier)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Langue.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BtnDescon)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BtnVoir)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BtnAjoutPanier)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdpageAfficherProduits }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdpageAfficherProduits }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdpageAfficherProduits })
        </td>
    </tr>
}

</table>
